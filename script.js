function setParagraphBackgroundColor(color) {
    // Select the paragraph by its ID and set its background color
    var paragraph = document.getElementById('myParagraph');
    paragraph.style.backgroundColor = color;
}

// Example usage: set the background color to 'lightblue'
setParagraphBackgroundColor('lightblue');
